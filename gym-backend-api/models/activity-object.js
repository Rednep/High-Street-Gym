export function Activity(
  activity_id,
  activity_name,
  activity_description,
  activity_duration,
  activity_image
) {
  return {
    activity_id,
    activity_name,
    activity_description,
    activity_duration,
    activity_image,
  };
}
